const ROOT = '/'
const JOIN = '/join'
const LOGIN = '/login'
const LOGOUT = '/logout'
const SEARCH = '/search'

const route = {
  root: ROOT,
  join: JOIN,
  login: LOGIN,
  logout: LOGOUT,
  search: SEARCH
}

export default route
